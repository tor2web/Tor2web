#!/usr/bin/env python
# -*- encoding: utf-8 -*-

import tor2web

import os
from subprocess import call

t2w = os.path.abspath(os.path.join(os.path.dirname(tor2web.__file__), 't2w.py'))

try:
    call(["twistd", "-n", "-y", t2w])
except:
    pass
